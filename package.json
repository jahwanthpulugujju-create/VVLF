{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --dir artifacts/vvlf-website dev",
    "build:website": "pnpm --dir artifacts/vvlf-website run build",
    "build": "pnpm --filter @workspace/vvlf-website run build && node -e \"const fs=require('fs'); fs.cpSync('artifacts/vvlf-website/dist', 'dist', {recursive: true});\"",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
